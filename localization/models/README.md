Place optional detector or depth-model files here if you want the localization package to use project-local weights.

The current implementation does not require any specific file in this folder.
It is provided so you can keep localization-specific models separate from the rest of slam_rover.